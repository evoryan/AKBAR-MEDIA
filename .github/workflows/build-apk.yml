name: Build Android APK

on:
  push:
    branches:
      - main  # Ganti ke master jika branch utama Anda bernama master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Diperbarui ke v4

      - name: Setup JDK
        uses: actions/setup-java@v4 # Diperbarui ke v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # Menginstal Gradle CLI langsung di server GitHub karena gradlew tidak ada
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      # Membuat file gradlew secara otomatis di dalam server
      - name: Generate Gradle Wrapper
        run: gradle wrapper

      - name: Make Gradle executable
        run: chmod +x gradlew

       - name: Build APK
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: "**/build/outputs/apk/debug/*.apk" # Jalur folder diubah ke debug
