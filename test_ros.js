const { RouterOSClient } = require('routeros-client');

async function test() {
    // Cannot really test since I don't have the password, but just checking syntax
    console.log("Syntax is OK");
}
test();
